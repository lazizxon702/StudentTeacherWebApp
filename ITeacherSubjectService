using StudentTeacher.DTOs;
using WebApplication1.Dto;

namespace WebApplication1.Interface;

 public interface ITeacherSubjectService
 {
    public Task<TeacherSubjectReadDto> GetAllTeacherSubjects();
    public Task<TeacherSubjectCreateDto> CreateAsync(TeacherSubjectCreateDto dto);

 }
